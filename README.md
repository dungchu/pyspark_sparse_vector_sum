# pyspark_sparse_vector_sum
Sum/adition of two pyspark sparse vectors

Add two sparse vectors

v1 = Vectors.sparse(3, {0: 1.0, 2: 1.0})

v2 = Vectors.sparse(3, {1: 1.0})

# Usage
add_sparsevector(v1, v2)
